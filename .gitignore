node_modules
.wrangler
.DS_Store
*.log
.env
.env.*
!.env.example

# Internal knowledge (agent init + migration notes) is versioned centrally in
# the projects-docs repo, never in this public repo.
CLAUDE.md
docs/
